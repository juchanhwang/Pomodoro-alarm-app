import React from 'react';
import { View, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: 312,
    height: 56,
    borderRadius: 16,
    backgroundColor: '#ffffff',
    borderWidth: 1,
  },
});

const RoutineInput = ({}) => <View style={styles.container} />;

export default RoutineInput;
